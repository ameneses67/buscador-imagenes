export const resultado = document.querySelector("#resultado") as HTMLDivElement;
export const formulario = document.querySelector(
  "#formulario"
) as HTMLFormElement;
export const inputBusqueda = document.querySelector(
  "#termino"
) as HTMLInputElement;
export const paginacionDiv = document.querySelector(
  "#paginacion"
) as HTMLDivElement;
